{
  "name": "valentine-countdown",
  "version": "1.0.0",
  "description": "A special Valentine's Day countdown website",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vercel --prod"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
